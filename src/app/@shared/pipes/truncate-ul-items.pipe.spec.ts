import { TruncateUlItemsPipe } from './truncate-ul-items.pipe';

describe('TruncateUlItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateUlItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
