import { TruncateUlItemsDirective } from './truncate-ul-items.directive';

describe('TruncateUlItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new TruncateUlItemsDirective();
    expect(directive).toBeTruthy();
  });
});
